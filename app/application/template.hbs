<Ui::PowerSelectMultiple
  @options={{this.people}}
  @selected={{this.selectedOptions}}
  @placeholder="Select the names"
  @onChange={{fn (mut this.selectedOptions)}}
  as |person|
>
  {{person.name}}  
</Ui::PowerSelectMultiple>
