<Ui::PowerSelectMultiple
  @options={{this.names}}
  @selected={{this.selectedName}}
  @placeholder="Select the names"
  @onChange={{fn (mut this.selectedName)}}
  as |name|
>
  {{name}}  
</Ui::PowerSelectMultiple>
